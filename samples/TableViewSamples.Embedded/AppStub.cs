namespace TableViewSamples;

// Minimal stand-in for the sample app's App helper. The embedded shell does not host the original
// Application; only the static selection-verification logging hook is referenced by sample pages.
internal static class App
{
    internal static void AppendSelectionVerificationLog(string message) { }
}
