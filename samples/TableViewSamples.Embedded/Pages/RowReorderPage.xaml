<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TableViewSamples.Pages.RowReorderPage"
    NavigationCacheMode="Enabled"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:controls="using:TableViewSamples.Controls">

    <controls:SamplePresenter
        Header="Row drag-and-drop reorder"
        Description="Drag any row by its cells to move it to a new position. CanUserReorderRows enables the gesture, and the RowReordered event reports the (from, to) index pair so you can apply the same move to your data model."
        SourceCaption="RowReorderPage.xaml (excerpt)"
        SourceSnippet="RowReorder.xaml.txt"
        StretchExample="True">

        <controls:SamplePresenter.Example>
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <muxc:InfoBar Grid.Row="0" IsOpen="True" IsClosable="False" Severity="Informational" Title="Try it" Message="Click and drag any row by its cells onto another row to drop it before or after. Toggle CanUserReorderRows to enable or disable the gesture."/>
                <Border Grid.Row="1" Style="{StaticResource TableHostBorderStyle}" VerticalAlignment="Stretch">
                    <muxc:TableView x:Name="PeopleTable" AutomationProperties.AutomationId="PeopleTable" SelectionMode="Single" CanUserReorderRows="True" CanUserReorderColumns="False" ItemsSource="{x:Bind People}" VerticalAlignment="Stretch" MinHeight="320">
                        <muxc:TableViewTextColumn Header="First name" Width="{StaticResource ColWidthShortName}" Binding="{Binding FirstName}" CanUserSort="False" CanUserFilter="False"/>
                        <muxc:TableViewTextColumn Header="Last name"  Width="{StaticResource ColWidthShortName}" Binding="{Binding LastName}" CanUserSort="False" CanUserFilter="False"/>
                        <muxc:TableViewTextColumn Header="Department" Width="{StaticResource ColWidthDepartment}" Binding="{Binding Department}" CanUserSort="False" CanUserFilter="False"/>
                        <muxc:TableViewTextColumn Header="Role"       Width="{StaticResource ColWidthLongName}" Binding="{Binding Role}" CanUserSort="False" CanUserFilter="False"/>
                        <muxc:TableViewTextColumn Header="Email"      Width="{StaticResource ColWidthEmail}" Binding="{Binding Email}" CanUserSort="False" CanUserFilter="False"/>
                    </muxc:TableView>
                </Border>
            </Grid>
        </controls:SamplePresenter.Example>

        <controls:SamplePresenter.Options>
            <StackPanel Spacing="12">
                <TextBlock Text="Reorder controls" Style="{StaticResource SectionHeaderTextStyle}"/>
                <TextBlock Text="Toggle CanUserReorderRows, then drag any row by its cells onto another row to drop it before or after. Column headers are fixed here, so the gesture always means a row reorder. The 'Last move' readout shows the (from, to) indices of the most recent move." Style="{StaticResource SectionDescriptionTextStyle}" TextWrapping="Wrap"/>
                <ToggleSwitch x:Name="ReorderToggle" AutomationProperties.AutomationId="ReorderToggle" Header="CanUserReorderRows" IsOn="True" OnContent="On" OffContent="Off" Toggled="OnReorderToggled"/>
                <StackPanel Spacing="8">
                    <Button x:Name="ResetButton" AutomationProperties.AutomationId="ResetButton" Content="Reset order" Click="OnResetClick" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"/>
                    <Button x:Name="DiagButton" AutomationProperties.AutomationId="DiagButton" Content="Diagnose" Click="OnDiagnoseClick" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"/>
                    <Button x:Name="MoveButton" AutomationProperties.AutomationId="MoveButton" Content="Move 0→3 (API)" Click="OnMoveClick" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="Status" Style="{StaticResource SectionHeaderTextStyle}" Margin="0,8,0,0"/>
                <Grid ColumnSpacing="12" RowSpacing="4">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="CanReorderRows" Style="{StaticResource StatusReadoutLabelStyle}"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="EffectiveGateText" Text="True" Style="{StaticResource StatusReadoutValueStyle}" AutomationProperties.AutomationId="EffectiveGateText"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Last move" Style="{StaticResource StatusReadoutLabelStyle}"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="LastMoveText" Text="(none)" Style="{StaticResource StatusReadoutValueStyle}" AutomationProperties.AutomationId="LastMoveText"/>
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Move count" Style="{StaticResource StatusReadoutLabelStyle}"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" x:Name="MoveCountText" Text="0" Style="{StaticResource StatusReadoutValueStyle}" AutomationProperties.AutomationId="MoveCountText"/>
                </Grid>
                <TextBlock Text="Diagnostics" Style="{StaticResource SectionHeaderTextStyle}" Margin="0,8,0,0"/>
                <TextBlock x:Name="DiagText" AutomationProperties.AutomationId="DiagText" Text="(press Diagnose)" TextWrapping="Wrap" FontFamily="Consolas" Style="{StaticResource StatusReadoutValueStyle}"/>
                <TextBlock x:Name="EventText" AutomationProperties.AutomationId="EventText" Text="(no events yet)" TextWrapping="Wrap" FontFamily="Consolas" Style="{StaticResource StatusReadoutValueStyle}"/>
            </StackPanel>
        </controls:SamplePresenter.Options>
    </controls:SamplePresenter>
</Page>
