---
title: "Reconciliation"
app: reconciliation
order: 33
audience: advanced
goal: |
  Element-record diff against the previous tree. Identity and WithKey.
  In-place mutation vs replace. Bitmask property diffs. The three
  phases: reconcile, mount/unmount, commit. Text, lists, conditional
  rendering. What ForEach actually does. Identity-preservation
  invariants. Promotes docs/reference/reconciliation.md. Authored in
  Phase 3.5.
tier: stub
---

# Reconciliation

Coming soon — this page explains how Reactor diffs the element-record
tree against the previous render: identity and `WithKey`, in-place
mutation vs replace, bitmask property diffs, the three phases
(reconcile, mount/unmount, commit), how `ForEach` preserves identity.
Promotes the existing `docs/reference/reconciliation.md` once authored.
See [the AI Author Skill](../../_pipeline/ai-author-skill.md) for the
planned surface.

Until then, [Components](components.md) covers the surface concepts.
