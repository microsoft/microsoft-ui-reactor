// Source-only doc topic — no runnable doc app. This stub exists because
// the doc compiler discovers app directories by presence of at least one
// .cs file (CompileCommand.DiscoverApps). The manifest beside it
// registers the topic's diagrams as screenshot:// references so tier-lint
// sees a resolved visual reference. All code on the page comes from
// snippet="source:..." against src/Reactor.Analyzers/.

namespace ReactorDocs.AnalyzerArchitecture;

internal static class Placeholder { }
