// Source-only doc topic — no runnable doc app. Required for the doc
// compiler to discover the app directory (CompileCommand.DiscoverApps
// looks for at least one .cs file before reading the manifest). All
// code samples on the page come from snippet="source:..." against
// src/Reactor/Animation/.

namespace ReactorDocs.AnimationPipeline;

internal static class Placeholder { }
